name: Test App with GitHub Actions
on:
  push:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Testing using jest after installing packages
        uses: actions/setup-node@v1
        with:
          node-version: "14"
      - run: npm install
      - run: npm test
